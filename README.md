Packages Intalled :
. npm install bootstrap
. npm install react-router-dom
. npm install axios
. npm install json-server
Run locally :
. npm install
. npm run dev
Run in another terminl
. npx json-server --watch db.json
